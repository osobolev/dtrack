rootProject.name = 'btrack'

def add(name) {
    def path = "subprojects/$name"
    def dir = file(path)
    include name
    project(":${name}").projectDir = dir
}

add('common')
add('dao_test')
add('web')
add('admin')
add('deploy')
